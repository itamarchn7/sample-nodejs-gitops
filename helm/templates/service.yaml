apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-nodejs.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sample-nodejs.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
